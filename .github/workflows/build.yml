name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install uv
      run: pip install uv
    
    - name: Install dependencies
      run: uv sync --all-extras
    
    - name: Build executable
      run: uv run pyinstaller --onefile --name "phone-locator" src/tracker/__main__.py
    
    - name: Rename binary (Linux)
      if: runner.os == 'Linux'
      run: mv dist/phone-locator dist/phone-locator-linux
    
    - name: Rename binary (macOS)
      if: runner.os == 'macOS'
      run: mv dist/phone-locator dist/phone-locator-macos
    
    - name: Rename binary (Windows)
      if: runner.os == 'Windows'
      run: mv dist/phone-locator.exe dist/phone-locator-windows.exe
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: phone-locator-${{ matrix.os }}
        path: dist/phone-locator-*

  create-release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts/
    
    - name: List downloaded artifacts
      run: find artifacts/ -type f
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        name: Phone Locator ${{ github.ref_name }}
        body: |
          ## Phone Locator ${{ github.ref_name }}
          
          CLI tool for phone number geolocation and carrier information.
          
          ### Downloads
          
          - **Linux**: `phone-locator-linux` - Compatible with most distributions
          - **macOS**: `phone-locator-macos` - Native for Apple Silicon/Intel
          - **Windows**: `phone-locator-windows.exe` - Windows 10/11 compatible
          
          ### Installation Options
          
          **Option 1: From PyPI**
          ```bash
          pip install phone-locator
          phone-locator
          ```
          
          **Option 2: Download executable**
          
          Linux/macOS:
          ```bash
          chmod +x phone-locator-*
          ./phone-locator-*
          ```
          
          Windows:
          ```cmd
          phone-locator-windows.exe
          ```
          
          ### What's Included
          - Phone number geolocation and analysis
          - Carrier information lookup
          - Timezone identification
          - Number validation and formatting
        files: |
          artifacts/phone-locator-ubuntu-latest/phone-locator-linux
          artifacts/phone-locator-macos-latest/phone-locator-macos
          artifacts/phone-locator-windows-latest/phone-locator-windows.exe
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
